html
    head  
        title Projeto lindo
    body
        h1 Users Form
        form(action="/submit", method="POST")
            input(type="text", name="userName", placeholder="User Name")
            input(type="text", name="userPhone", placeholder="User Phone")
            button(type="submit") Submit
